console.log("Hello World!");

// Let and const only works on scope level and var works on global level.

const name = "Texas";

// Only global truth value are assigned capital naming
// we cannot break higher order functions like map and forEach
// === checks both value and data types
// complex data object uses memory references to assign values than primitive data which uses only assigned values
